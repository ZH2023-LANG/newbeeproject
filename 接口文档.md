# NewBee Mall 项目接口文档

## 1. 说明
- 项目类型：Spring Boot + Thymeleaf 混合应用（页面渲染 + JSON 接口）。
- 返回类型：
  - 页面路由：返回模板名（`String`）。
  - JSON接口：主要返回 `Result`（统一结构：`resultCode`/`message`/`data`）。
- 登录态：
  - 管理端：`/admin/**` 依赖后台登录 session。
  - 商城端：购物车、订单、个人中心等依赖 `MALL_USER_SESSION_KEY`。

---

## 2. 公共模块接口（common）

### 2.1 验证码
- `GET /common/kaptcha`
  - 说明：生成后台登录验证码图片（PNG），并将验证码对象写入 session 的 `verifyCode`。
- `GET /common/mall/kaptcha`
  - 说明：生成商城登录/注册验证码图片（PNG），并写入 `Constants.MALL_VERIFY_CODE_KEY`。

### 2.2 文件上传（管理端）
- `POST /admin/upload/file`
  - 参数：`multipart/form-data`，字段 `file`
  - 返回：`Result`，成功时 `data` 为图片 URL。
  - 约束：只接受图片文件。

- `POST /admin/upload/files`
  - 参数：`multipart/form-data`，多文件
  - 返回：`Result`，成功时 `data` 为 URL 列表。
  - 约束：最多 5 张，只处理图片类型。

---

## 3. 管理端接口（admin）

### 3.1 管理员认证与资料
- `GET /admin/login`：后台登录页
- `POST /admin/login`
  - 参数：`userName`、`password`、`verifyCode`
  - 返回：登录成功重定向 `/admin/index`，失败返回登录页并写 session 错误信息。
- `GET /admin/index`、`GET /admin/`、`GET /admin`：后台首页
- `GET /admin/profile`：个人资料页
- `POST /admin/profile/password`
  - 参数：`originalPassword`、`newPassword`
  - 返回：`String`（成功返回 `SUCCESS`）
- `POST /admin/profile/name`
  - 参数：`loginUserName`、`nickName`
  - 返回：`String`
- `GET /admin/logout`：退出登录

### 3.2 轮播图管理
- `GET /admin/carousels`：管理页
- `GET /admin/carousels/list`
  - 参数：`page`、`limit`
  - 返回：分页 `Result`
- `POST /admin/carousels/save`
  - Body：`Carousel`
- `POST /admin/carousels/update`
  - Body：`Carousel`
- `GET /admin/carousels/info/{id}`：详情
- `POST /admin/carousels/delete`
  - Body：`Integer[] ids`

### 3.3 分类管理
- `GET /admin/categories`：管理页
- `GET /admin/categories/list`
  - 参数：`page`、`limit`、可选筛选参数
- `GET /admin/categories/listForSelect`
  - 参数：`categoryId`、`level`
  - 说明：用于级联选择
- `POST /admin/categories/save`
  - Body：`GoodsCategory`
- `POST /admin/categories/update`
  - Body：`GoodsCategory`
- `GET /admin/categories/info/{id}`
- `POST /admin/categories/delete`
  - Body：`Integer[] ids`

### 3.4 商品管理
- `GET /admin/goods`：商品列表页
- `GET /admin/goods/edit`：新增页
- `GET /admin/goods/edit/{goodsId}`：编辑页
- `GET /admin/goods/list`
  - 参数：`page`、`limit`
- `POST /admin/goods/save`
  - Body：`NewBeeMallGoods`
- `POST /admin/goods/update`
  - Body：`NewBeeMallGoods`
- `GET /admin/goods/info/{id}`
- `PUT /admin/goods/status/{sellStatus}`
  - Body：`Long[] ids`
  - 说明：上下架

### 3.5 首页配置管理（热销/新品/推荐）
- `GET /admin/indexConfigs`：管理页
- `GET /admin/indexConfigs/list`
  - 参数：`page`、`limit`、`configType`
- `POST /admin/indexConfigs/save`
  - Body：`IndexConfig`
- `POST /admin/indexConfigs/update`
  - Body：`IndexConfig`
- `GET /admin/indexConfigs/info/{id}`
- `POST /admin/indexConfigs/delete`
  - Body：`Long[] ids`

### 3.6 用户管理
- `GET /admin/users`：管理页
- `GET /admin/users/list`
  - 参数：`page`、`limit`
- `POST /admin/users/lock/{lockStatus}`
  - Path：`lockStatus`（0/1）
  - Body：`Long[] ids`

### 3.7 订单管理
- `GET /admin/orders`：管理页
- `GET /admin/orders/list`
  - 参数：`page`、`limit`、筛选参数
- `POST /admin/orders/update`
  - Body：`NewBeeMallOrder`
- `GET /admin/order-items/{id}`
  - 说明：按订单主键查明细
- `POST /admin/orders/checkDone`
  - Body：`Long[] ids`（配货完成）
- `POST /admin/orders/checkOut`
  - Body：`Long[] ids`（出库）
- `POST /admin/orders/close`
  - Body：`Long[] ids`（关闭订单）

---

## 4. 商城前台接口（mall）

### 4.1 首页与商品
- `GET /`、`GET /index`、`GET /index.html`
  - 说明：首页，包含分类、轮播、热销、新品、推荐。
- `GET /search`、`GET /search.html`
  - 参数：`keyword`、`page`、排序/分类等可选参数
  - 说明：商品搜索页
- `GET /goods/detail/{goodsId}`
  - 说明：商品详情页，仅上架商品可查看

### 4.2 用户系统
- `GET /login`、`GET /login.html`：登录页
- `POST /login`
  - 参数：`loginName`、`password`、`verifyCode`
  - 返回：`Result`
- `GET /register`、`GET /register.html`：注册页
- `POST /register`
  - 参数：`loginName`、`password`、`verifyCode`
  - 返回：`Result`
- `GET /logout`：退出
- `GET /personal`：个人中心页
- `GET /personal/addresses`：地址页
- `POST /personal/updateInfo`
  - Body：`MallUser`
  - 返回：`Result`

### 4.3 购物车
- `GET /shop-cart`：购物车页
- `POST /shop-cart`
  - Body：`NewBeeMallShoppingCartItem`
  - 说明：新增购物项
- `PUT /shop-cart`
  - Body：`NewBeeMallShoppingCartItem`
  - 说明：修改购物项
- `DELETE /shop-cart/{newBeeMallShoppingCartItemId}`
  - 说明：删除购物项
- `GET /shop-cart/settle`
  - 说明：结算页

### 4.4 订单与支付
- `GET /saveOrder`
  - 说明：从购物车创建订单，成功后重定向 `/orders/{orderNo}`
- `GET /orders`
  - 参数：`page`
  - 说明：我的订单列表页
- `GET /orders/{orderNo}`：订单详情页
- `PUT /orders/{orderNo}/cancel`：取消订单（JSON）
- `PUT /orders/{orderNo}/finish`：确认收货（JSON）
- `GET /selectPayType`
  - 参数：`orderNo`
  - 说明：支付方式选择页
- `GET /payPage`
  - 参数：`orderNo`、`payType`
  - 说明：根据 `payType` 跳转支付宝或微信支付页
- `GET /paySuccess`
  - 参数：`orderNo`、`payType`
  - 返回：`Result`，标记支付成功

---

## 5. 异常与错误页
- 全局异常处理：
  - Ajax/JSON 请求：返回 `Result(resultCode=500, message=...)`
  - 页面请求：返回 `error/error`
- 错误状态页映射：
  - 400 -> `error/error_400`
  - 404 -> `error/error_404`
  - 其他 -> `error/error_5xx`

---

## 6. 统一返回体（JSON）
多数 AJAX 接口使用 `Result`：
- `resultCode`：状态码（如 200 成功、500 失败）
- `message`：提示信息
- `data`：业务数据（分页对象、详情对象、URL 列表等）

---

## 7. 接口梳理来源
- Controller 目录：
  - `src/main/java/ltd/newbee/mall/controller/common/*`
  - `src/main/java/ltd/newbee/mall/controller/admin/*`
  - `src/main/java/ltd/newbee/mall/controller/mall/*`

